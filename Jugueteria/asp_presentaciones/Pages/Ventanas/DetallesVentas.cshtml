@page
@model asp_presentaciones.Pages.Ventanas.DetallesVentasModel
@{
    ViewData["Titulo"] = "DetallesVentas";
}

<center>
    <div>
        <form method="POST" enctype="multipart/form-data">
            @Html.AntiForgeryToken()

            <div align="left">
                <h3>Detalles de Venta</h3>
            </div>
            <div align="left" class="bt_panel_buttons">
                <input type="submit" value="Refrescar" asp-page-handler="BtRefrescar" class="btn btn-secondary" disabled="@(Model.Accion != lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />
                <input type="submit" value="Nuevo" asp-page-handler="BtNuevo" class="btn btn-secondary" disabled="@(Model.Accion != lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />
                <input type="submit" value="Guardar" asp-page-handler="BtGuardar" class="btn btn-secondary" disabled="@(Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />
                <input type="submit" value="Cancelar" asp-page-handler="BtCancelar" class="btn btn-secondary" disabled="@(Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Listas)" />
            </div>

            @if (Model.Accion != lib_dominio.Nucleo.Enumerables.Ventanas.Listas || Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
            {
                <input type="hidden" asp-for="@Model.Actual!.Id" />
                <img src="@Url.Content("~/imagenes/20250525_0419_image (1).png")" alt="DetallesVentas" />
            }
            @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
            {
                <input type="hidden" asp-for="@Model.Actual!.Cantidad" />
                <input type="hidden" asp-for="@Model.Actual!.Preciounitario" />
                <input type="hidden" asp-for="@Model.Actual!.ventas" />
                <input type="hidden" asp-for="@Model.Actual!.juguetes" />
            }

            @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Editar || Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Editar)
            {
                <div class="containers_vn panel panel-default">
                    <h3>Detalle de Venta</h3>
                    <div class="form-group">
                        <label class="col-lg-4 control-label">Cantidad</label>
                        <div class="col-lg-6">
                            <input type="number" class="cts_forms form-control" asp-for="@Model.Actual!.Cantidad" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-lg-4 control-label">Precio Unitario</label>
                        <div class="col-lg-6">
                            <input type="number" step="any" class="cts_forms form-control" asp-for="@Model.Actual!.Preciounitario" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-lg-4 control-label">Venta ID</label>
                        <div class="col-lg-6">
                            <select asp-for="Actual!.ventas"
                                    asp-items="@(new SelectList(Model.Venta, "Id", "Nombre"))" class="form-control">
                                <option value="">Selecciona una Venta</option>
                                <option value="1">uno</option>
                                <option value="2">dos</option>
                                <option value="3">tres</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-lg-4 control-label">Juguete ID</label>
                        <div class="col-lg-6">
                            <select asp-for="Actual!.juguetes"
                                    asp-items="@(new SelectList(Model.Juguete, "Id", "Nombre"))" class="form-control">
                                <option value="">Selecciona un Juguete</option>
                                <option value="1">uno</option>
                                <option value="2">dos</option>
                                <option value="3">tres</option>
                            </select>
                        </div>
                    </div>
                </div>
            }

            @if (ViewData.ContainsKey("Mensaje"))
            {
                <input type="hidden" asp-for="@Model.Accion" />
                @await Html.PartialAsync("../Emergentes/Mensajes.cshtml")
            }

            @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
            {
                <div class='popup'>
                    <div class='pp_del_inner'>
                        <div style='width:95%'>
                            <h2 align="left">Mensaje</h2>
                            <h4 align="left">Desea borrar el detalle de venta?</h4>
                            }<h6 align="left">Cantidad: @Model.Actual!.Cantidad, PrecioUnitrio: @Model.Actual!.Preciounitario, Ventas ID: @Model.Actual!.ventas,Juguete ID:@Model.Actual!.juguetes</h6>
                            <br /><br />
                            <input type="submit" value="Borrar" asp-page-handler="BtBorrar" class="btn btn-secondary" />
                            <input type="submit" value="Cerrar" asp-page-handler="BtCancelar" class="btn btn-secondary" />
                        </div>
                    </div>
                </div>
            }

            @if (Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Listas || Model.Accion == lib_dominio.Nucleo.Enumerables.Ventanas.Borrar)
            {
                <div class="containers_vn panel panel-default">
                    <table class="tb_data table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th>Cantidad</th>
                                <th>Precio Unitario</th>
                                <th>ID Venta</th>
                                <th>ID Juguete</th>
                                <th>Acción</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (Model != null && Model.Lista != null)
                            {
                                @foreach (var elemento in Model.Lista)
                                {
                                    <tr>
                                        <td>@elemento.Cantidad</td>
                                        <td>@elemento.Preciounitario</td>
                                        <td>@elemento._Id_Venta!.Fecha</td>
                                        <td>@elemento._Id_Juguete!.Nombre</td>
                                        <td>
                                            <input type="submit" value="Modificar" asp-page-handler="BtModificar" class="btn btn-secondary" asp-route-data="@elemento.Id" />
                                            <input type="submit" value="Borrar" asp-page-handler="BtBorrarVal" class="btn btn-secondary" asp-route-data="@elemento.Id" />
                                        </td>
                                    </tr>
                                }
                            }
                        </tbody>
                    </table>
                </div>
            }
        </form>
    </div>
</center>
